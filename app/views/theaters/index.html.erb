<p id="notice"><%= notice %></p>

<h1>Listing Theaters</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th>Address</th>
      <th>Theater Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @theaters.each do |theater| %>
      <tr>
        <td><%= theater.name %></td>
        <td><%= theater.location %></td>
        <td><%= theater.address %></td>
        <td><%= link_to 'Show', theater %></td>
        <td><%= link_to 'Edit', edit_theater_path(theater) %></td>
        <td><%= link_to 'Destroy', theater, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>





<footer><%= link_to 'Home', root_path %></footer>
<footer><%= link_to 'New Theater', new_theater_path %></footer>
<footer><%= link_to 'Back to Movie Page', '/movies' %></footer>
